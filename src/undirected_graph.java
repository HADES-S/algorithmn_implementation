import java.util.*;

/**
 * Created by Administrator on 2017/3/4.
 */
/*
public class undirected_graph {
    class UndirectedGraphNode {
        int label;
        ArrayList<UndirectedGraphNode> neighbors;
        UndirectedGraphNode(int x) { label = x; neighbors = new ArrayList<UndirectedGraphNode>(); }
    }
    Set<UndirectedGraphNode> oldset = new HashSet<UndirectedGraphNode>();
    Set<UndirectedGraphNode> newset = new HashSet<UndirectedGraphNode>();
    Queue<UndirectedGraphNode> oldqueue = new LinkedList<UndirectedGraphNode>();
    Queue<UndirectedGraphNode> newqueue = new LinkedList<UndirectedGraphNode>();
    public UndirectedGraphNode cloneGraph(UndirectedGraphNode node) {
        oldqueue.add(node);
        oldqueue
                .add()
        while(!queue.isEmpty()){
            UndirectedGraphNode NewNode = queue.poll();
            if(!oldset.contains(NewNode)){

            }
            oldset.add(NewNode);
            for(tempNode : NewNode.neighbors){
                if()
            }
        }
    }
    public UndirectedGraphNode clone()
    {
        UndirectedGraphNode temp = new UndirectedGraphNode(this.label);
    }
    public UndirectedGraphNode findNode(Set<UndirectedGraphNode> set,int label){
        for (UndirectedGraphNode node :set){
            if(node.label==label)
                return node;
        }
        return null;
    }
}
*/